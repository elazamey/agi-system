{
  "name": "@agi-os/providers",
  "version": "0.1.0",
  "private": true,
  "description": "Provider Broker — Cost-free provider management with fallback routing and CostGuard $0 enforcement",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      },
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "@agi-os/kernel": "workspace:*",
    "@agi-os/self-model": "workspace:*"
  },
  "devDependencies": {
    "@agi-os/governance": "workspace:*",
    "@agi-os/memory": "workspace:*",
    "@agi-os/reflection": "workspace:*",
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  }
}
